meta:
  description: Offline demo that echoes prompts; no network required.
  secrets: []
  safety: Safe; actions disabled.
transports:
  - type: mock
    id: mock
agent:
  type: echo
  config:
    model: echo
    timeout_seconds: 10
actions: []
runner:
  allowed_pubkeys: []
  session_timeout_minutes: 60
